[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen-a_maze_ing"
version = "0.1.0"
description = "Maze generator"
authors = [
    { name = "aunoguei" },
    { name = "ppousser" }
]
requires-python = ">=3.11"